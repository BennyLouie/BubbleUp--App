<% if flash[:errors] %>
  <div id="error_explanation" style="font-weight: bold;">
    <h2><%= pluralize(flash[:errors].count, "error") %> prohibited this review from being saved:</h2>
      <% flash[:errors].each do |message| %>
        <p style="color: black;"><%= message %></p>
      <% end %>
  </div>
<% end %>

<%= form_for @shop do |form| %>
  <div class="field">
    <%= form.label :name, class: "label" %>
    <%= form.text_field :name, class: "input" %>
  </div>

  <div class="field">
    <%= form.label :location, class: "label" %>
    <%= form.text_field :location, class: "input" %>
  </div>

  <div class="field">
    <%= form.label :address, class: "label" %>
    <%= form.text_field :address, class: "input" %>
  </div>

  <div class="button" >
    <%= form.submit 'Submit Shop', class: "button-is-primary" %>
  </div>
<% end %>
